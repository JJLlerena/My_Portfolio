import { Container } from 'react-bootstrap';
import styles from '../styles.module.css'

function About() {
    return(
        <Container className={styles['component-body']}>
         
        </Container>
    );
}

export default About